from __future__ import annotations

import pandas as pd


def preprocess_heart_dataframe(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for heart dataset preprocessing pipeline."""
    return df.copy()
